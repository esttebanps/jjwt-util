package com.esttebanps;


import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;

import javax.crypto.SecretKey;

import java.util.List;

import static com.esttebanps.JwtUtil.*;

public class Main {
    public static void main(String[] args) {
//        System.out.println("--------------- Main ---------------");
//
//        String username = "esttebanps";
//        String[] roles = new String[] { "admin", "user"};
//        SecretKey secretKey = Jwts.SIG.HS256.key().build();
//        int duration = 1;
//
//        String token = createToken(username, roles, secretKey, duration);
//        System.out.println("Token creado: " + token);
//
//        boolean isValid = isTokenValid(token, secretKey);
//        System.out.println("Token válido: " + isValid);
//
//        String usernameFromToken = getUsernameFromToken(token, secretKey);
//        System.out.println("Username del token: " + usernameFromToken);
//
//        List rolesFromToken = getRolesFromToken(token, secretKey);
//        System.out.println("Roles del token: " + rolesFromToken);

    }
}